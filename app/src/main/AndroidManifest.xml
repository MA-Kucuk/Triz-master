<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.malikucuk.triz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Matrix" />
        <activity android:name=".cards" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".EskiProje40matris.BulusPrensip40Activity" />
        <activity android:name=".EskiProje40matris.ParcalarDetayKaydir" />
        <activity android:name=".AyriliDetailActivity" />
        <activity android:name=".AyrilikPrensipleriActivity"></activity>
    </application>

</manifest>